apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-init-script
  namespace: emartapp-application
data:
  MONGO_URI: "mongodb://admin:example@mongo-service:27017/epoc?authSource=admin"
  init-db.js: |
    db = db.getSiblingDB('epoc');
    db.createCollection("users");
    print("Database epoc initialized");