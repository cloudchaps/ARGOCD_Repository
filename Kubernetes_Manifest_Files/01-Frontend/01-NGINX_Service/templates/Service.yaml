apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.servicename }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.appname }}

  ports:
  - port: 80
    targetPort: 80

  type: ClusterIP