apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: emartapp-application
spec:
  selector:
    app: client-service

  ports:
  - port: 4200
    targetPort: 4200

  type: ClusterIP